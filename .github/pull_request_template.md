## What

<!-- One-line description of the change. -->

## Why

<!-- Motivation — link to task/issue if applicable. -->

## Checklist

- [ ] `npm run typecheck` passes locally
- [ ] `npm run lint` passes locally
- [ ] `npm test` passes locally
- [ ] New logic has unit tests (or isn't testable in isolation — explain why)
- [ ] No secrets, tokens, or credentials committed
- [ ] `bpsai-pair arch check` passes on changed files
