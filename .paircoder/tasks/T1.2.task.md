---
id: T1.2
title: Database Schema Migration
plan: plan-2026-04-budget-app-mvp
type: chore
priority: P0
complexity: 40
status: done
sprint: 1
tags:
- infra
- backend
depends_on:
- T1.1
completed_at: '2026-04-11T13:52:19.231492'
---

# Objective

Write Supabase SQL migrations for all 9 tables with FK constraints, enums, CHECK constraints (including `classification_config` single-row lock), RLS policies, and seeded data. Every other task depends on this schema being correct.

# Implementation Plan

- `001_schema.sql`: create enums, then 9 tables in dependency order: `user` → `bill` → `bucket` → `week` → `bucket_allocation` → `transaction` → `bill_status` → `schedule_parse` → `classification_config`
- `002_rls.sql`: `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on all tables; add `SELECT/INSERT/UPDATE/DELETE` policies scoped to `user_id = auth.uid()`
- `003_seed.sql`: insert the single `classification_config` row with all seed_examples from spec

Key constraints to implement:
- `classification_config` CHECK: `id = 'a0000000-0000-0000-0000-000000000001'`
- All monetary fields as `integer` (cents)
- `user.setup_complete boolean DEFAULT false`
- `bucket.deficit_floor_pct` nullable (null only for flex)
- `transaction.week_id` and `transaction.bill_id` nullable FKs

# Acceptance Criteria

- [x] All 9 tables present with correct column types (integer cents, uuid PKs, decimal percentages)
- [x] `classification_config` single-row CHECK prevents second row insertion
- [x] `classification_config` seeded: `auto_assign_threshold=0.85`, `flag_threshold=0.60`, `food_weekly_minimum=5000`, full `seed_examples` JSONB
- [x] RLS enabled on all tables; policies enforce `user_id = auth.uid()`
- [x] `bucket.type` enum: `bills`, `debt`, `savings`, `food`, `flex`, `custom`
- [x] `week.status` enum: `projected`, `active`, `closed`; `bill_status.status` enum: `unpaid`, `teller_confirmed`, `manually_confirmed`
- [x] `user.setup_complete boolean DEFAULT false` present
- [x] Migrations idempotent (`IF NOT EXISTS`)

# Verification

- `supabase db reset` applies all migrations cleanly
- Attempt to insert second `classification_config` row → DB error
- Unauthenticated query on any table returns 0 rows

# Key Files

- `supabase/migrations/001_schema.sql`
- `supabase/migrations/002_rls.sql`
- `supabase/migrations/003_seed.sql`