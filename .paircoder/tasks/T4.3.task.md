---
id: T4.3
title: Architecture Compliance Pass
plan: plan-2026-04-budget-app-mvp
type: chore
priority: P0
complexity: 30
status: in_progress
sprint: 4
tags:
- infra
depends_on:
- T4.1
- T4.2
---

# Objective

Run `bpsai-pair arch check` across the full `src/` tree, resolve all violations, and ensure the check exits clean before MVP is considered done.

# Implementation Plan

- Run: `bpsai-pair arch check src/`
- For each error (> 400 lines) or warning (> 200 lines): split the file into logical sub-modules
- For function length violations (> 50 lines): extract named sub-functions
- For import count violations (> 20): reorganize barrel imports or split modules
- For function count violations (> 15/file): extract helpers to separate utility files
- Common split candidates (planned during implementation):
  - Engine files: already split by design (T2.2 plan)
  - Teller client: already split by design (T2.1 plan)
  - Page components: extract sub-components if page.tsx grows large
- Document any edge case where a split would make code worse — none expected

# Acceptance Criteria

- [ ] `bpsai-pair arch check src/` exits 0 errors, 0 warnings
- [ ] No file in `src/` exceeds 400 lines
- [ ] All API routes ≤ 15 functions (typically 1 exported GET/POST handler)
- [ ] No function body > 50 lines
- [ ] Engine files use named sub-functions (already planned in T2.2)

# Verification

- `bpsai-pair arch check src/` → clean output with 0 errors, 0 warnings

# Key Files

- All files in `src/` (cross-cutting check)